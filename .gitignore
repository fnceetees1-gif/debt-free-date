node_modules/
.expo/
dist/
web-build/
*.log
npm-debug.*
yarn-debug.*
yarn-error.*

# Native build output (managed workflow regenerates these)
ios/
android/

# Secrets — never commit
.env
.env.local
*.p8
*.p12
*.mobileprovision
*.keystore

# Personal / third-party data — this repo is PUBLIC (github.com/fnceetees1-gif/debt-free-date)
# These hold other people's email addresses, business identifiers, and a home
# address. None of it belongs in a public repository.
OUTREACH_DRAFTS.md
OUTREACH_NATIONAL.md
OUTREACH_TRACKER.md
# Catch-all, so a new outreach file can never be committed by being named
# something the list above does not anticipate.
OUTREACH_*.md
outreach_tracker.csv
EMAIL_TEST.md
ANDROID_RELEASE.md
BUSINESS_IDS.md

# OS
.DS_Store
Thumbs.db
